# Motion Imprints (parent application)

Independent Next.js app for the Motion Imprints parent website.

- Dev: `npm run dev -w motion-imprints` (port 3000)
- See the repository root `README.md` for setup and deployment.
